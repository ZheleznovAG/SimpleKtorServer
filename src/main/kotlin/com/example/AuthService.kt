package com.example

class AuthService {
    fun processToken(token: String): String {
        // Логика обработки токена, например, извлечение данных пользователя
        return "Processed token: $token"
    }
}